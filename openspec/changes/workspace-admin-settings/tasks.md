## 1. UI Infrastructure & Navigation

- [x] 1.1 Update CSS variables to ensure `--color-accent` is `#2563eb` and add helper styles for toggles and form controls.
- [x] 1.2 Add "Workspace Administration" section and routes (Business Hours, Holiday List) to the sidebar in `settings-page.html`.
- [x] 1.3 Implement JavaScript routing logic to switch between settings pages by toggling visibility.
- [x] 1.4 Set "Business Hours" as the default active route on page load.

## 2. Business Hours Page Implementation

- [x] 2.1 Create the HTML structure for the Business Hours page content in `settings-page.html`.
- [x] 2.2 Implement the primary Enable/Disable toggle UI.
- [x] 2.3 Add configuration fields: Target Inboxes, Time Zone, and Holiday Selection.
- [x] 2.4 Implement the Operational Hours selector (24/7 vs. Specific Business Hours).
- [x] 2.5 Add the weekly schedule grid for "Specific Business Hours" mode.

## 3. Holiday List Page Implementation

- [x] 3.1 Create the HTML structure for the Holiday List page content.
- [x] 3.2 Implement a list view for holidays and a button to add new holidays.

## 4. Verification

- [x] 4.1 Verify that clicking sidebar links switches content correctly.
- [x] 4.2 Ensure all interactive elements use the `#2563eb` blue theme.
- [x] 4.3 Confirm the "Business Hours" page is selected by default on load.
